rules.sqlite = {
  path = "/tmp/telemetry.db"     # SQLite database file path
  rules = [
    {
      sql = "SELECT topic, payload FROM \"#\""   # SQL clause to capture all topics and payloads
      table = "telemetry"                    # Table name to store everything
    }
  ]
}
