rules.sqlite = {
  path = "/tmp/telemetry.db"     # SQLite database file path
  rules = [
    {
      sql = "SELECT payload FROM \"sensor/house_temperature\""
      table = "house_temperature"
    },
    {
        sql = "SELECT payload FROM \"sensor/heart_rate\""
        table = "heart_rate"
    }
  ]
}
